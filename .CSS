/* Reset some default styles to ensure consistent styling */
body, h1, h2, h3, p, ul, li {
    margin: 0;
    padding: 0;
}

/* Apply your chosen color scheme */
body {
    background-color: beige;
    font-family: Arial, sans-serif;
    color: #333; /* Dark gray text */
}

header {
    background-color: white;
    padding: 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

#logo img {
    max-width: 150px; /* Adjust as needed */
}

nav ul {
    list-style-type: none;
}

nav ul li {
    display: inline;
    margin-right: 20px;
}

nav a {
    text-decoration: none;
    color: #333; /* Dark gray text */
    font-weight: bold;
}

/* Style the homepage section */
#home {
    text-align: center;
    padding: 50px;
}

.buy-now-button {
    display: inline-block;
    padding: 15px 30px;
    background-color: #FF5733; /* Orange */
    color: white;
    text-decoration: none;
    font-weight: bold;
    border-radius: 5px;
    transition: background-color 0.3s ease;
}

.buy-now-button:hover {
    background-color: #FF704D; /* Slightly lighter orange on hover */
}

/* Style the About Us section */
#about {
    padding: 30px;
}

/* Style the Dolls section */
#dolls {
    padding: 30px;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
}

.product {
    flex: 1;
    margin: 10px;
    padding: 20px;
    background-color: white;
    border: 1px solid #ccc;
    border-radius: 5px;
    text-align: center;
}

.product img {
    max-width: 100%;
    border-radius: 5px;
}

.buy-now-button {
    display: block;
    margin-top: 10px;
}

/* Style the Contact Us section */
#contact {
    padding: 30px;
}

/* Style the Social Media section */
#social {
    background-color: white;
    padding: 30px;
    text-align: center;
}

#social a {
    display: inline-block;
    margin: 10px;
    text-decoration: none;
    color: #FF5733; /* Orange */
    font-weight: bold;
    transition: color 0.3s ease;
}

#social a:hover {
    color: #FF704D; /* Slightly lighter orange on hover */
}

/* Style the footer */
footer {
    background-color: #333; /* Dark gray background */
    color: white;
    text-align: center;
    padding: 10px;
}
